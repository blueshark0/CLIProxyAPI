### 当前请求如何处理

  Claude 上游返回非 2xx 时，执行器保留 HTTP 状态码和响应体，交给认证管理器处理：internal/runtime/executor/claude_executor.go:237。

  一次下游请求中：

  1. 当前认证失败，立即记录模型失败状态。
  2. 当前认证加入本次请求的 tried 集合。
  3. 选择另一个支持该模型且未冷却的认证继续请求。
  4. 当前配置 max-retry-credentials: 0，因此会尝试所有可用认证文件。
  5. 所有认证都失败后，只有冷却等待不超过 30 秒时，才可能等待并重新进行下一轮。

  当前配置是：config.yaml:82

  request-retry: 3
  max-retry-credentials: 0
  max-retry-interval: 30
  disable-cooling: false

  即最多是“初始一轮 + 3 轮重试”，但能否重试取决于错误对应的冷却时间。sdk/cliproxy/auth/conductor.go:1226

  ### 不同失败的处理

   失败类型                        对该认证的该模型进行的处理
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   401                             冷却 30 分钟，切换其他认证
  ──────────────────────────────  ────────────────────────────────────────────────
   402/403                         冷却 30 分钟
  ──────────────────────────────  ────────────────────────────────────────────────
   普通 404                        冷却 12 小时
  ──────────────────────────────  ────────────────────────────────────────────────
   明确“不支持该模型”的 400/422    冷却 12 小时
  ──────────────────────────────  ────────────────────────────────────────────────
   429                             指数冷却：1 秒、2 秒、4 秒、8 秒……最高 30 分钟
  ──────────────────────────────  ────────────────────────────────────────────────
   408/500/502/503/504             冷却 1 分钟
  ──────────────────────────────  ────────────────────────────────────────────────
   网络错误或未分类错误            不设置冷却；下次请求仍可立即选中
  ──────────────────────────────  ────────────────────────────────────────────────
   请求本身非法的 400/422          不切换认证，直接向下游返回错误

  对应分类逻辑在 sdk/cliproxy/auth/conductor.go:2296，429 退避逻辑在 sdk/cliproxy/auth/conductor.go:2838。

  Claude 执行器目前没有从上游响应头提取 Retry-After，所以 Claude 的 429 通常采用内置的指数退避。按照当前配置，同一请求连续遇到 429 时，通常会等待 1 秒、2 秒、4 秒后重试；退避超过 30 秒后，本次请求不再等待。

  ### 连续失败的实际影响

  - 第一次失败就进入冷却，不需要达到某个次数。
  - Failed 计数会递增，但它只是统计数据，不用于永久熔断。
  - 只有这个认证的这个模型被跳过；同一认证上的其他模型仍然可以使用。选择器按 ModelStates[model] 判断：sdk/cliproxy/auth/selector.go:371。
  - 冷却时间结束后，该模型会重新参与选择。
  - 下一次成功会清除错误、冷却和 429 退避级别。
  - 如果再次失败，非 429 错误重新设置固定冷却；429 会继续增大退避时间。
  - 如果所有 Claude 认证对该模型都在冷却或不可用，最终向下游返回最后一次上游错误或“无可用认证”。

  另外，这些模型冷却状态主要保存在进程内的 Auth.ModelStates 中：sdk/cliproxy/auth/types.go:90。默认文件存储写入的是认证 Metadata，不会把 ModelStates 写进 OAuth 认证 JSON：sdk/auth/filestore.go:73。因此完整重启后，这些运行时冷却状态通常不
  会从认证文件恢复。

  需要特别区分：以上是“模型请求失败”。如果是后台 OAuth token 刷新本身返回 401，则可能把整个认证标记为 unauthorized，并停止继续自动刷新，这属于另一条处理链路。
